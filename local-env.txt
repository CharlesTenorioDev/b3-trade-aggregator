# B3 Trade Aggregator - Local Environment Configuration
# This file will be automatically renamed to .env if .env doesn't exist
# Copy this file to .env and modify the values as needed

# Application Configuration
APP_NAME=BACKEND-APPLICATION
SRV_PORT=8080
SRV_MODE=DEVELOPER

# Database Configuration
SRV_DB_HOST=localhost
SRV_DB_NAME=b3_trade_aggregator
SRV_DB_USER=postgres
SRV_DB_PASS=postgres
SRV_DB_PORT=5432
SRV_DB_SSL_MODE=disable

# Alternative: Use DATABASE_URL instead of individual variables
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/b3_trade_aggregator?sslmode=disable

# API Configuration
API_PORT=8080

# Data File Path
FILE_PATH=data/29-08-2025_NEGOCIOSAVISTA.txt

# Logging Configuration
LOG_LEVEL=info
LOG_OUTPUT=stdout